# Fallback guard if someone invokes `npm` instead of `pnpm`.
# Authoritative pnpm v11+ project config lives in `pnpm-workspace.yaml`
# (`minimumReleaseAge: 10080` = 7 days in minutes; `ignoreScripts: true`).
min-release-age=7
ignore-scripts=true
